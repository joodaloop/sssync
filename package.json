{
  "name": "sssync-beta",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.7.0",
  "scripts": {
    "size": "node --experimental-strip-types scripts/bundle-size.ts",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "sonda": "^0.11.1",
    "vite": "^8.0.13"
  }
}
